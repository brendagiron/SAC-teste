/** @type {import('next').NextConfig} */
// Define o tipo de configuração do Next.js para o arquivo

const nextConfig = {
    reactStrictMode: true, // Habilita o modo estrito do React para ajudar a identificar problemas durante o desenvolvimento
    // Outras configurações podem ser adicionadas aqui conforme necessário
  };
  
  // Exporta a configuração para que o Next.js possa usá-la
  module.exports = nextConfig;
  